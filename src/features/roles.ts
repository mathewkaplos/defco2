/**
 * Roles are customizable. Add or rename roles.
 * Make sure you replace the names in the code.
 * You may want to update src/features/permissions.ts as well.
 * */
export const roles = {
  admin: 'admin',
  custom: 'custom',
} as const;
